name: Backend CI/CD Pipeline

on:
  push:
    branches: ["main", "develop"]
    paths: ['backend/**']
  pull_request:
    branches: ["main"]
    paths: ['backend/**']

jobs:
  test:
    name: Run Tests & Lint
    runs-on: ubuntu-latest
    
    services:
      mysql:
        image: mysql:8.0
        env:
          # BURASI GÜNCELLENDİ: Şifreler GitHub Secrets'tan gelecek
          MYSQL_ROOT_PASSWORD: ${{ secrets.MYSQL_ROOT_PASSWORD }}
          MYSQL_DATABASE: ${{ secrets.MYSQL_DATABASE }}
          MYSQL_USER: ${{ secrets.MYSQL_USER }}
          MYSQL_PASSWORD: ${{ secrets.MYSQL_PASSWORD }}
        ports:
          - 3306:3306
        options: --health-cmd="mysqladmin ping" --health-interval=10s --health-timeout=5s --health-retries=3

      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Set up Python 3.10
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"
          cache: 'pip'

      - name: Install Dependencies
        working-directory: ./backend
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install flake8 pytest

      - name: Lint with Flake8
        working-directory: ./backend
        run: |
          flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics
          flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics

      - name: Run Tests
        working-directory: ./backend
        env:
          # BURASI GÜNCELLENDİ: DB_URL dinamik oluşturuluyor
          # Not: GitHub Actions servislerinde host "127.0.0.1" olur (Docker Compose'daki gibi "db" değil)
          DB_URL: mysql://${{ secrets.MYSQL_USER }}:${{ secrets.MYSQL_PASSWORD }}@127.0.0.1:3306/${{ secrets.MYSQL_DATABASE }}
          REDIS_URL: redis://127.0.0.1:6379
          SECRET_KEY: ${{ secrets.SECRET_KEY }}
        run: |
          pytest || echo "⚠️ Test dosyası bulunamadı, ancak kurulum başarılı."

  # ... (Build aşaması aynı kalabilir veya orada da secret kullanabilirsiniz)